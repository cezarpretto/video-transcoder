# Vídeo transcoder FFmpeg

## Dependecies

- Install FFmpeg
    - Linux
        ```sh
        $ apt-get install ffmpeg
        ```
    - MacOS
        ```sh
        $ brew install ffmpeg 
        ```

 - Install node dependencies
    ```sh
    $ npm install
    ```

## Running
```sh
$ node index.js
```
